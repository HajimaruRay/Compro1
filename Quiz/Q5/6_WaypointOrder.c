#include <stdio.h>

void main()
{
    int rol,col;
    scanf("%d%d",&rol,&col);
    int arr[rol+1][col+1];

    for (int i = 1;i < rol+1;i++)
    {
        for (int j = 1;j < col+1;j++)
        {
            arr[i][j] = 0;
        }
    }

    int k;
    scanf("%d",&k);
    for (int i = 0;i < k;i++)
    {
        
    }
}